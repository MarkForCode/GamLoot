# Clippy configuration only accepts lint option keys here.
# Lint levels belong in Cargo/rustc flags, so CI passes `-D warnings`.
avoid-breaking-exported-api = false
msrv = "1.82"
too-many-arguments-threshold = 10
